{
  "name": "multi-tenant-crm-saas",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "npm run dev --workspace backend",
    "dev:backend": "npm run start:dev --workspace backend",
    "dev:frontend": "npm run start --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "build:vercel": "npm run prisma:generate && npm run build --workspace backend && npm run build:prod --workspace frontend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "prisma:generate": "npm run prisma:generate:master --workspace backend && npm run prisma:generate:tenant --workspace backend",
    "prisma:seed": "npm run prisma:seed --workspace backend",
    "atlas:master:diff": "npm run atlas:master:diff --workspace backend",
    "atlas:master:apply": "npm run atlas:master:apply --workspace backend",
    "atlas:tenant:diff": "npm run atlas:tenant:diff --workspace backend",
    "atlas:tenant:apply": "npm run atlas:tenant:apply --workspace backend"
  }
}
